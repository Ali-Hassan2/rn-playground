import { StyleSheet } from 'react-native';

const TextStyles = StyleSheet.create({
  text: {
    fontSize: 30,
    fontWeight: '700',
    marginBottom: 16,
    textAlign: 'center',
    color: 'crimson',
  },
});

export { TextStyles };
